// Firebase設定のデバッグユーティリティ（無効化）
export const debugFirebaseConfig = () => {
    // デバッグログを完全に無効化
    return
}

// Firebase接続エラーの詳細分析（無効化）
export const analyzeFirebaseError = (error: any) => {
    // エラー分析ログを完全に無効化
    return
}